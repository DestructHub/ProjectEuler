language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
  - "3.5"

language: go
go:
  - '1.4'
  - '1.5'
  - '1.6'

# useless for while...
#
# command to install dependencies
# install:
#   - pip install .
#   - pip install -r requirements.txt
# command to run tests
# script: nosetests




before_install:
  - gem install rspec
  - sudo apt-get install python-numpy python-scipy python-pandas golang python3-pip python-pip python-sympy
  - sudo pip3 install numpy pandas cython
  - echo '{:user {:plugins [[lein-exec "0.3.3"]]}}'' > ~/.lein/profiles.clj
  - sudo wget https://raw.githubusercontent.com/technomancy/leiningen/stable/bin/lein > /usr/bin/lein


after_success:
  - python3 stats.py --search python --search go --build